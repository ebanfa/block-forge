syntax = "proto3";
package davail.davail;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "davail/x/davail/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "davail/x/davail/Params";
  option (gogoproto.equal) = true;

  
}