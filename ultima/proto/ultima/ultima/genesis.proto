syntax = "proto3";
package ultima.ultima;

import "amino/amino.proto";
import "gogoproto/gogo.proto";
import "ultima/ultima/params.proto";

option go_package = "ultima/x/ultima/types";

// GenesisState defines the ultima module's genesis state.
message GenesisState {
  // params defines all the parameters of the module.
  Params params = 1 [
    (gogoproto.nullable) = false,
    (amino.dont_omitempty) = true
  ];
}
